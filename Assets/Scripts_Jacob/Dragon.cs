using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Dragon : MonoBehaviour {

	Vector3 position;
	Vector3[] history;
	int[] body_segments;
	int segment_space;

	// Use this for initialization
	void Start () {
		
		body_segments = [10];
		for( int i = 0 ; i < body_segments.GetLength(0) ; i ++ ){
			//Instantiate( )
		}

		segment_space = 100;

		history = new Vector3[body_segments.GetLength(0) * segment_space];
	}
	
	// Update is called once per frame
	void Update () {

		for( int i = history.GetLength(0) ; i > 0 ; i -- ){

			history[i] = history[i-1];

		}
		history[0] = position;

		for( int i = 0 ; i < body_segments.GetLength(0) ; i ++ ){
			//body_segment[i].position = history[(i+1) * segment_space ]
		}
		
	}
}
*/